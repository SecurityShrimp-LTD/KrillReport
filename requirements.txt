# --- Core runtime dependencies ---------------------------------------------
pydantic>=2.5
pydantic-settings>=2.1
PyYAML>=6.0
lxml>=5.0

# Document ingestion / generation
python-docx>=1.1          # DOCX read (template branding) + write (report rendering)
pdfplumber>=0.10          # PDF ingestion (text + tables) and best-effort branding
weasyprint>=60            # PDF rendering from HTML/CSS (needs system pango/cairo libs)
Jinja2>=3.1               # HTML templating for PDF + web UI
Pillow>=10.0              # image handling for logos / evidence screenshots

# Web backend + CLI
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9   # multipart form uploads for FastAPI
click>=8.1

# --- Optional LLM providers -------------------------------------------------
# Only required if you select the matching provider. The offline provider (default)
# needs none of these. Install the one(s) you use, e.g. `pip install anthropic`.
anthropic>=0.39           # provider: anthropic
openai>=1.30              # provider: openai
httpx>=0.27               # provider: ollama (and general HTTP)
